# SQLite (async). Relative paths are resolved from the backend working directory.
DATABASE_URL=sqlite+aiosqlite:///./project_pilot.db

# Frontend dev server (Vite default). Used by CORS.
# CORS_ORIGINS is parsed as JSON in code if extended later; defaults cover localhost:5173.

# Optional — Phase 2 (AI summary)
# OPENAI_API_KEY=
